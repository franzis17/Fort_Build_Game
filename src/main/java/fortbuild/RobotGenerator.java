package fortbuild;

import javafx.scene.canvas.*;

public class RobotGenerator
{
    private GraphicsContext gfx;  // Used for drawing the robot
    
    public RobotGenerator(GraphicsContext gfx)
    {
        this.gfx = gfx;
    }
    
    // Randomly spawn a robot every 1.5 seconds in 1 of the 4 corners
}
